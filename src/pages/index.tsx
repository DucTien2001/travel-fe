import type { NextPage } from "next";
import dynamic from "next/dynamic";
import LandingPage from "./landingPage";


const Home: NextPage = () => {
  return (
        <LandingPage />
  );
};

export default Home;
